       // IN operator
       if MyVar in %list(w : y : z );
         // or
       if MyVar = w or MyVar = y or MyVar = z;
       
       if MyVar in %range(y : z);
         // or
       if MyVar >= y and MyVar <= z;
