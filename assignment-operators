       // assignments
        a += b;
          // or
        a = a + b;

        a -= b;
          // or
        a = a – b;

        a *= b;
          // or
        a = * – b;
        
        a /= b;
          // or
        a = a / b;